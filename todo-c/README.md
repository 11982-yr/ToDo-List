# To-Do List in C (CLI)

A simple command-line to-do list app written in C.  
Tasks are saved to `tasks.txt` so they persist between runs.

## Features
- Add tasks
- List tasks
- Toggle done/undone
- Delete tasks
- Auto save/load

## Build & Run
```bash
gcc src/main.c -o todo
./todo
